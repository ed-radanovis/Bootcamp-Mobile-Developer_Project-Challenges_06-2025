// method for "Direct Export", in this way we already point out that the functions / constants / etc. have already been created exportable

export const key = {
  value: "🤘_666-rocK-888-Roll",
  permission: "admin",
};

export const getDataFromApi = async () => {
  console.log("searching API data ...\n");
};
